# Phase Risk Board

Single-page exec demo over `output/phase_state.csv`.

```bash
python3 scripts/bake_data.py   # refresh src/data/phases.json from pipeline output
npm install
npm run dev                    # http://localhost:5173
```
